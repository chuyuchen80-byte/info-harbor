# info-harbor

聚合全球多源 AI 动态，经 LLM 筛选评估后按国家/地区维度展示。
FastAPI 全异步栈后端 + Vue3 前端。

## 怎么理解这个项目

在根目录工作时，先读 `backend/CLAUDE.md`——它会指引按顺序阅读
`backend/docs/` 下的 PROJECT / ARCHITECTURE / DOCKER / DEVELOPMENT /
CURRENT_STATUS / DEVELOPER_PROFILE 等文档。

前端涉及面有限，需要时直接读 `frontend/src/` 相关代码。

## 规则

- 后端详细规则都在 `backend/CLAUDE.md` 和 `backend/docs/` 中，按需查阅，不要凭记忆猜
- 改代码后同步更新对应 docs（对照表见 `backend/docs/AI_AGENT_GUIDE.md`）
